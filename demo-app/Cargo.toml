[package]
name = "demo-app"
version = "1.0.0"
edition = "2021"

[dependencies]

# Base de données : sqlite (défaut) | postgres | mysql | mariadb | all-databases
# en prod => runique = { version = "1.0.86", features = ["orm", "postgres"] }
# en local
runique = { path = "../runique", features = ["orm", "postgres"] }

serde ="1"

chrono = "0.4"
tracing-subscriber.workspace = true
